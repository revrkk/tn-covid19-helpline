name: covid19
description: TN Covid19 Helpline
publish_to: 'none'
version: 0.0.1+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  stacked: ^2.1.1
  stacked_services: ^0.8.5
  http: ^0.12.1
  get_it: 
  injectable:
  url_launcher: ^5.5.2
  share: ^2.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  injectable_generator:
  build_runner:

flutter:
  uses-material-design: true

  assets:
    - assets/images/
